import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { Analytics } from "@vercel/analytics/next";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "GB | Software Engineer",
  description: "Building modern, high-performance web applications. View my projects, skills, and experience.",
  keywords: ["Gbolahan Oni", "frontend developer", "React developer", "Next.js developer", "web developer Nigeria", "UI/UX design", "JavaScript developer", "TypeScript", "Tailwind CSS", "Framer Motion", "responsive web design", "portfolio website", "remote frontend developer", "freelance web developer", "Osun State developer"],
  icons: {
    icon: "/favicon.ico",
    apple: "/apple-touch-icon.png",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <head>
        <link
          rel="icon"
          href="/favicon.ico"
        />
      </head>
      <body className="min-h-full flex flex-col">
        {children}
        <Analytics />
      </body>
    </html>
  );
}
